trigger KeyRelationship on Relationship__c (after insert, after update,after delete) {
    /*
    KeyRelHandler KRH=new KeyRelHandler();
    
    if(trigger.isDelete){
      KRH.proce(Trigger.old,1) ;
    }
    
    if(trigger.isInsert==True){
        KRH.proce(Trigger.new,2);
    }
    
      if(trigger.isUpdate==True){
        KRH.proce(Trigger.old,1);
        KRH.proce(Trigger.new,2);
    }
    */
  
}